import React from 'react';

const WebSocketInstanceContext = () => {
    const WebSocketInstanceContext = React.createContext();
    return WebSocketInstanceContext;
};

export default WebSocketInstanceContext();
